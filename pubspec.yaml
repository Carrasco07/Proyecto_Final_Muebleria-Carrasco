name: myapp
description: "Proyecto Mueblería Carrasco"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  lottie: ^3.3.3
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2
  go_router: ^17.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/  # Esto cargará tu calavera.json